# Data2Decision: Project Description Analyzer (NLP)

This project is a professional Machine Learning system designed to analyze GitHub project descriptions (like READMEs) and provide intelligent career insights. It identifies core skills present in a project and suggests missing skills or new projects to improve a developer's portfolio.

## 📁 Project Structure

```text
project_root/
│
├── data/
│   ├── raw/                # Original, immutable datasets
│   └── processed/          # Data transformed for model training
│
├── models/
│   ├── skill_classifier.pkl   # Multi-label classifier (OneVsRest + RandomForest)
│   └── tfidf_vectorizer.pkl   # TF-IDF vectorization model
│
├── notebooks/              # Jupyter notebooks for experimentation and EDA
│
├── src/                    # Core source code
│   ├── __init__.py         # Package initializer
│   ├── config.py           # Centralized paths and hyperparameters
│   ├── data_preprocessing.py # Text cleaning and splitting
│   ├── feature_engineering.py # TF-IDF transformation
│   ├── train.py            # Model training and serialization
│   ├── evaluate.py         # Performance assessment
│   ├── predict.py          # Isolated inference
│   ├── recommend.py        # Logic-based career insights
│   └── main.py             # Pipeline orchestrator
│
├── reports/                # Generated performance reports and visualizations
├── logs/                   # System and training logs for debugging
│
├── requirements.txt
└── README.md
```

### 🔹 Repository Structure Explanation

*   **data/raw vs data/processed**: We separate incoming raw data from the data prepared for the model to ensure a reproducible pipeline and protect the integrity of the original source.
*   **src/ modules**: Contains the functional logic of the project, including data handling, feature extraction, and the orchestration of the ML pipeline.
*   **models/**: Dedicated storage for serialized model artifacts. Centralizing these allows different versions to be managed independently of the source code.
*   **logs/**: Used to track the execution history and catch errors during training or inference.
*   **reports/**: Stores the final analysis results, accuracy reports, and any visual insights generated by the system.

---

### 🔹 Data Flow Mapping

The system follows a linear end-to-end pipeline:

1.  **Raw Data**: Fetched from `data/raw/`.
2.  **Preprocessing**: Text is cleaned and normalized in `data_preprocessing.py`.
3.  **Feature Engineering**: Cleaned text is converted to numerical vectors using TF-IDF in `feature_engineering.py`.
4.  **Model Training**: A Multi-label classifier is trained in `train.py`.
5.  **Evaluation**: Success metrics are calculated in `evaluate.py`.
6.  **Model Saving**: Trained objects are serialized to the `models/` directory.
7.  **Prediction**: New project descriptions are analyzed in `predict.py` using stored artifacts.
8.  **Recommendation**: Career insights are generated in `recommend.py` based on predicted skills.

---

### 🔹 Design Justification

*   **Raw and Processed Separation**: Maintaining a `raw/` directory ensures that the original data remains a "single source of truth." Processed data can be regenerated at any time using the preprocessing scripts, which prevents permanent errors during data cleaning from corrupting the source.
*   **Notebooks outside src**: Jupyter Notebooks are excellent for Exploratory Data Analysis (EDA) and prototyping, but they are difficult to version control and test. Keeping them separate from the `src/` directory ensures that the "production" code remains clean and maintainable.
*   **Models outside src**: Models are large binary artifacts, not source code. Storing them in a dedicated `models/` directory follows the separation of code and data principle, making it easier to manage model versions and deployment.
*   **Logs and Reports**: Professional systems require auditability. Logs provide transparency into system behavior during long-running tasks, while reports allow stakeholders to review model performance without digging into the code.
*   **Centralized config.py**: Hardcoding paths across multiple files is a major source of refactoring bugs. Using a `config.py` allows the entire project environment (paths, constants, hyperparameters) to be adjusted from a single location.

---

## 🛠️ Environment Setup & Reproducibility

### 1. Requirements
- **Python Version**: 3.9+
- **Platform**: Windows, macOS, or Linux

### 2. Creation & Activation
We recommend using **Conda** for isolated environment management to ensure that library versions do not conflict with other projects.

**Using Conda (Recommended):**
```bash
# Create the environment
conda create -n ml_env python=3.9 -y

# Activate the environment
conda activate ml_env
```

### 3. Install Dependencies
Install the pinned versions of the required libraries:
```bash
pip install -r requirements.txt
```

### 4. Run the Pipeline
Execute the full end-to-end analysis through the orchestrator:
```bash
python -m src.main
```

---

## ⚡ Core Features

### NLP Logic
The system uses **TF-IDF Vectorization** to convert project descriptions into numerical features. It then employs a **Multi-Output Classifier** to detect multiple skills from a single block of text.

### Intelligent Recommendations
The `recommend.py` module analyzes detected skills against industry standards to identify gaps and suggest the next stage of portfolio growth.

---

## 📝 Assignment: Supervised Learning Problem Type Analysis

This repository includes an additional module, `src/problem_type_analysis.py`, which explores various Supervised Learning scenarios and provides practical examples of Classification and Regression.

### 🔹 Classification vs. Regression
- **Classification**: Predicts discrete categories or labels (e.g., "Fraud" or "Not Fraud").
- **Regression**: Predicts continuous numerical values (e.g., House Prices or Temperature).

### 🔹 Scenario Analysis Summary
1.  **Fraud Detection**: Binary Classification. Accuracy is often misleading due to imbalanced data; Recall and Precision are prioritized.
2.  **House Price Prediction**: Regression. Success is measured by how close predictions are to actual prices (MAE, RMSE).
3.  **Movie Genre Tagging**: Multi-Label Classification. Films often span multiple genres simultaneously.
4.  **Product Demand Prediction**: Regression. Forecasts quantity to optimize inventory.
5.  **Disease Classification**: Multi-Class Classification. Categorizes symptoms into distinct medical diagnoses.

### 🔹 Implementation Highlights
- **Synthetic Data**: Uses `sklearn.datasets` to generate reproducible examples.
- **Model Evaluation**: Demonstrates essential metrics like F1-Score for classification and R² for regression.
- **Independent Module**: This analysis runs separately from the core NLP pipeline via `python src/problem_type_analysis.py`.

---

## Feature and Target Definition

### Target Variable
- **Column**: `skills` (Conceptual set represented by `Python`, `SQL`, `React`, `Machine_Learning`, `Flask`)
- **Type**: Multi-label classification
- **Meaning**: Technologies and skills detected within the project description.

### Features
- **project_description**: The primary text input (cleaned GitHub project descriptions).

### Excluded Columns
- **project_id** → unique identifier (removed to prevent the model from memorizing specific projects).
- **manual_tags** → leakage prevention (removed because it contains human-labeled skill tags that wouldn't be available during inference).

### Problem Type
- **Multi-label classification**: Each project can be associated with multiple skills simultaneously.

### Evaluation Metrics
- **F1 score (primary)**: Balances Precision and Recall for multi-label tasks.
- **Precision / Recall**: Essential for understanding the trade-off between skill detection accuracy and coverage.

### Leakage Prevention
- **Only input text used**: Features are derived strictly from the `project_description`.
- **No post-output features**: Any columns created after or alongside the skill tags (like `manual_tags`) are strictly excluded during training.

---

## Feature Distribution Analysis

### Numerical Insights
- **Text Length Distribution**: Most project descriptions are around 50-60 characters, showing a relatively tight distribution with low variance.
- **Word Count Skewness**: The distribution of word counts is slightly right-skewed (0.5084), indicating a few projects with significantly longer descriptions.
- **Outliers**: There are minimal outliers in the current small dataset, but the boxplot identifies projects with 10+ words as the upper-end range.

### Text Insights
- **Most Common Words**: "python", "using", "built", and "react" are the most frequent keywords, aligning with the project's focus.
- **Keyword Patterns**: Projects consistently describe the "tools" and "purpose" (e.g., "management", "scripts").

### Target Insights
- **Skill Distribution Imbalance**: "Python" is the most dominant skill (60% presence), while "Flask" is the least frequent (20% presence).
- **Most Common Skills**: Python, SQL, and React form the majority of the dataset labels.

### Target Comparison
- **Longer descriptions → more skills**: Generally, projects with 8+ words tend to have more associated skill labels than shorter ones.
- **Pattern observations**: ML and Backend projects (Python/Flask) often share similar word distributions compared to purely frontend tasks.

### Transformations (if needed)
- **No scaling needed for text**: NLP features like TF-IDF inherently normalize term frequencies.
- **TF-IDF handles normalization**: It accounts for document length variations automatically.

### Leakage Prevention
- **Only input text used**: All analysis is derived strictly from the `project_description`.
- **No future information included**: `manual_tags` and `project_id` are completely ignored during distribution analysis.

---

## Data Splitting Strategy

- **Dataset Split**: 80% training and 20% testing
- **Method**: Random split used (not time-based)
- **Reproducibility**: `random_state=42` used for consistent results
- **Multi-label Handling**: Stratification was not used due to the complex nature of multi-label tag combinations.
- **Leakage Prevention**: No preprocessing (cleaning, TF-IDF, or feature engineering) was applied before the split.
- **Test Set Integrity**: The test set was kept entirely untouched during the training phase to ensure a fair evaluation of model performance.

---

## Data Leakage Demonstration

### Leakage Type
Train-Test Contamination (via Feature Scaling)

### What Was Done Wrong
In the contamination scenario, `StandardScaler` was fitted and transformed on the **entire dataset** before performing the train-test split. This allowed the training process to "see" the mean and variance of the test data, leading to statistical contamination.

### Impact
Numerical leakage causes the model to have an unfair advantage, often resulting in **artificially high or deceptive accuracy** that will not persist when the model encounters real-world data from a different distribution.

### Fix
The correct workflow is to **Split first**, then fit the scaler ONLY on the training data. The test data should then be transformed using the statistics (mean/std) learned strictly from the training set.

### Key Learning
A Machine Learning model must only learn from information that would be available at prediction time. Any "look-ahead" into the test set's distribution violates the core principle of objective evaluation.

---

## Feature Type Definition

### Target Variable
- **Column**: `skills`
- **Type**: Multi-label classification
- **Meaning**: Technologies and skills detected within the project description.

---

### Numerical Features
- **None in raw dataset**: Standard numerical metrics (like age or price) are not present in the raw GitHub project data.
- **Transformational Numericality**: Numerical representation is generated dynamically using TF-IDF during the preprocessing phase.

---

### Categorical Features
- **None in raw dataset**: There are no label-based categorical columns (like 'City' or 'Category') used as input features in this project.
- **No label-based categorical columns**: The model relies entirely on unstructured text.

---

### Text Features
- **project_description**: This is the primary input feature.
- **NLP Processing**: Converted into high-dimensional numerical vectors using TF-IDF before being passed to the classifier.

---

### Excluded Columns
- **project_id** → identifier (removed as it has no predictive value and leads to overfitting).
- **manual_tags** → potential data leakage (removed to ensure objective performance evaluation).

---

### Edge Case Handling
- **No binary columns present** in features.
- **No ordinal features** detected.
- **No high-cardinality categorical features** to handle.
- **Text handled separately** using a dedicated NLP pipeline.


---

## Model Evaluation using MAE

### What is MAE?
- **Mean Absolute Error**: It represents the average magnitude of the errors in a set of predictions, without considering their direction.
- **Unit Consistent**: It is measured in the exact same units as the target variable, making it highly intuitive for stakeholders.

### Results
- **Baseline MAE**: 105.11
- **Model MAE**: 15.65
- **Improvement**: 85.11% reduction in average error.

### Interpretation
- Model predictions improved by **89.46 units** on average compared to a naive mean-predictor.
- The MAE of 15.65 shows the "real-world" average mistake the model makes when estimating the continuous target.

### RMSE Insight
- **RMSE** is invariably higher than MAE (usually around ~20 in this demo).
- This indicates that while the average error is low, there are some "larger" outliers in the predictions that the squared-error penalty of RMSE is highlighting.

### R² Insight
- **R² Score (0.98)**: Indicates that 98% of the target's variance is successfully explained by our features.

### Cross Validation
- **Mean MAE**: 16.35 ± 0.94
- **Stability**: The low standard deviation (0.94) across 5 folds proves the model is stable and not sensitive to specific data splits.

### Key Learning
- **Intuition**: MAE is the easiest metric to explain to non-technical users.
- **Context**: An MAE of 15 means nothing without the baseline context (105).
- **Holistic View**: Never rely on a single metric; use R² for variance and RMSE for outlier sensitivity.

---

## Evaluation using MSE and R²

### What is MSE?
- **Mean Squared Error**: Measures the average of the squares of the errors.
- **Penalty Mechanism**: By squaring the differences, MSE penalizes large mistakes much more heavily than small ones, making it ideal for detecting outliers.

### What is R²?
- **Coefficient of Determination**: Measures the proportion of the variance in the target that is predictable from the features.
- **Baseline Comparison**: An R² of 0 indicates the model is no better than guessing the mean (the baseline).

### Results
**Baseline:**
- **MSE**: 17,133.35
- **RMSE**: 130.89
- **R²**: ~0.00 (Zero predictive utility)

**Model:**
- **MSE**: 214.01
- **RMSE**: 14.62
- **R²**: 0.98 (Excellent fit)

### Interpretation
- **Lower MSE/RMSE**: Indicates that the model's predictions are tightly clustered around the true values.
- **Higher R²**: Confirms that the model successfully learned 98% of the data's structural relationships.

### Cross Validation
- **Mean R²**: 0.9873 ± 0.0028
- **Mean RMSE**: 14.66 ± 0.57
- **Stability**: The extremely low standard deviation in both R² and RMSE indicates a highly stable and reliable model.

### Key Insight
- **MSE** tells us the magnitude of the error (penalizing the "worst cases").
- **R²** tells us the overall "usefulness" of the model compared to a naive guess.
- Both must be used together to ensure the model is both accurate and consistent.

---

## Video Answer: Key Regression Questions

### Q1: Is the model better?
**👉 YES.** Every metric shows a massive improvement. The MSE dropped by nearly 98%, and R² jumped from zero to near-perfect.

### Q2: What if R² = 0.20?
**👉 Weak/Moderate.** It means the model only captures 20% of the information. While technically better than a baseline, it’s not yet reliable for critical decisions.

### Q3: What if R² is negative?
**👉 Critical Failure.** A negative R² means the model is actually performing worse than a naive mean-predictor. This usually happens due to severe overfitting or a highly inappropriate model choice.

### Q4: High CV variation?
**👉 Unstable.** If the R² fluctuates wildly between folds (e.g., 0.9 to 0.4), the model is "lucky" on some data and "lost" on others. It lacks generalization power.

### Q5: Should we deploy a model with R²=0.20?
**👉 ❌ Not yet.** 20% variance explanation is rarely enough for production. It needs more features, better preprocessing, or a more complex algorithm to be truly effective.

---

## Video Answer: Advanced Evaluation Nuances

### Q1: Is the model better?
**👉 YES.** The reduction in MAE from 105 to 15 is a definitive proof of learning.

### Q2: Why is RMSE always higher?
**👉 RMSE penalizes outliers.** Because RMSE squares the errors before averaging them, even a few large mistakes will pull the RMSE far above the MAE.

### Q3: What if R² = 0.12?
**👉 Weak Model.** It means the model is barely doing better than a random guess. Only 12% of the story is being told by your variables.

### Q4: What if CV variation is high?
**👉 Model Unstable.** If the MAE jumps from 10 to 50 across different folds, your model is likely overfitting or the data is too small/noisy to generalize.

### Q5: Is only MAE enough?
**👉 ❌ NO.** MAE tells you the average error, but R² tells you the trend, and RMSE tells you about the risk of large failure. You need all three for a complete picture.

---

### Key Insight
Feature types in this project are defined based on how the model interprets data (unstructured vs. structured), not just how the data is stored (strings vs. numbers). This allows the system to focus on the semantic meaning of project descriptions.

---

## Numerical Feature Scaling

### In This Project
- **No numerical features in raw dataset**: The primary data is unstructured text (GitHub READMEs).
- **Text is converted using TF-IDF**: This transforms words into a numerical representation.
- **TF-IDF already normalizes data**: The weighting scheme (TF * IDF) inherently accounts for document length and term frequency, making additional scaling like `StandardScaler` unnecessary and potentially harmful to sparsity.

### Scaling Decision
- **StandardScaler NOT applied** to the main multi-label classification pipeline.

### Demonstration
- A separate demonstration (`src/scaling_demo.py`) was implemented to show the effect of scaling on distance-based models like **Logistic Regression**.
- **Result**: Accuracy improved from ~81% to ~87% when scaling was applied to synthetic data with varied ranges, demonstrating that scaling improves optimization stability for gradient-based solvers.

### Leakage Prevention
- **Split before Scaling**: The `StandardScaler` is fitted only on the training set.
- **Transformed Test Set**: The test data is transformed using the mean and standard deviation learned strictly from the training data, preventing information from the future from entering the training process.

---

## Feature Normalization using MinMaxScaler

### In This Project
- **No numerical features present**: The raw dataset consists of textual descriptions.
- **Text is transformed using TF-IDF**: The resulting vectors already represent word importances in a normalized comparative scale.
- **TF-IDF already normalizes values**: Standard TF-IDF implementations in `scikit-learn` use L2 normalization by default.

### Why MinMaxScaler Not Used
- **NLP features already scaled**: Applying another scaling layer to TF-IDF sparse matrices is redundant.
- **No distance-based raw numerical features**: Our current Random Forest model is tree-based and does not rely on Euclidean distances.

### Demonstration
- A separate demonstration (`src/minmax_scaling_demo.py`) shows the effect of squeezing features into a **range of [0, 1]**.
- **Result**: Verification of `min=0.0000` and `max=1.0000` on the training set, ensuring correct boundary implementation.

### Leakage Prevention
- **Split before Scaling**: Scaling parameters were learned strictly from the training set.
- **Consistent Transformation**: The test set was transformed using the same scaler object without re-fitting.

### When to Use MinMaxScaler
- **Distance-based models**: k-Nearest Neighbors (kNN), Support Vector Machines (SVM).
- **Optimization stability**: Artificial Neural Networks (ANN) where consistent activation ranges improve backpropagation.

---

## Baseline Model Comparison

### Baseline Strategy
- **DummyClassifier**: Used the `most_frequent` strategy, which always predicts the class that appears most often in the training data.

### Why Baseline?
Establish a minimum performance benchmark to determine if the trained model actually adds predictive value beyond a naive guess.

### Results
- **Baseline Accuracy**: 0.8500
- **Model Accuracy**: 0.9000
- **Improvement**: +0.0500 (Significant for imbalanced data)

### Key Insight
The baseline shows a very high accuracy (85%) despite having zero intelligence. This demonstrates why the model must significantly outperform the baseline and show strength in other metrics to be considered "useful."

### Important Learning
- **Accuracy is misleading**: In imbalanced datasets, accuracy can be high even if the model fails to find anything.
- **F1 Score importance**: Our model's F1 score rose from **0.00** (dummy) to **0.67** (intelligent), proving it captured patterns rather than just guessing.

---

## Video Answer: Understanding Model Evaluation

**👉 Why is the 85% baseline misleading?**
The dataset is imbalanced. If 85% of projects are 'Python', a model that just says 'Python' every time with zero thought will get 85% accuracy. It's not learning; it's just repeating.

**👉 Is our model better?**
**YES**. While accuracy only improved by 5%, our **Recall and F1 Score** improved dramatically. The model is now capable of identifying the minority classes that the baseline ignored completely.

**👉 Which metric is most important?**
**Recall and F1 Score.** In multi-label skill prediction, we care more about finding all the relevant skills (Recall) than just being generally right about the majority class.

**👉 If accuracy is 89% but recall is 0.05?**
**❌ BAD MODEL.** This model is essentially a "fancy baseline." It hide its failure to detect important minority classes behind a high accuracy number. It would fail in a real-world scenario where those minority skills are critical.

---

## Linear Regression Model

### Baseline
- **DummyRegressor**: Used the `mean` strategy. 
- **Significance**: Represents a naive prediction where we always guess the average value of the target variable.

### Model
- **Linear Regression**: A standard statistical model that learns the linear relationship between features and a continuous target.
- **Pipeline Implementation**: Integrated `StandardScaler` to ensure features are on a consistent scale before reaching the regressor.

### Results
- **Baseline RMSE**: 130.91
- **Model RMSE**: 15.11
- **Improvement**: +115.80 (Significant error reduction)
- **R² Score**: 0.986 (Excellent fit)

### Interpretation
**R² (Coefficient of Determination) Meaning**:
- **0** → The model is only as good as the baseline (guessing the mean).
- **> 0** → The model has learned structural patterns and improved over the mean.
- **< 0** → The model is actually worse than just guessing the mean.

### Key Insights
- The model shows massive improvement over the baseline, justifying its complexity.
- A high R² score suggests the synthetic data has a strong linear bias that the model captured.
- Metrics like RMSE provide a concrete idea of the "average error" in the target's specific units.

---

## Video Answer: Regression Evaluation

### Q1: Is the model better?
**👉 Yes**. We can confirm this because the **RMSE** and **MAE** decreased significantly, while the **R² Score** increased from zero to nearly 1.0.

### Q2: What if R² = 0.11?
**👉 This suggests a weak model.** It means the features only explain 11% of the variation in the target. 89% of the behavior is "noise" or unexplained by the current features.

### Q3: What if CV results vary widely?
**👉 The model is unstable.** This indicates poor generalization—the model might be overfitting to specific chunks of data and would likely fail on brand-new data.

### Q4: What if residuals increase with the target value?
**👉 Heteroscedasticity**. This means one of the core assumptions of Linear Regression (constant variance of error) is violated. The model is less reliable for higher values of the target.

### Q5: Should we deploy a model with R²=0.11 and high variance?
**👉 ❌ No.** A model that is both weak and unstable is dangerous for production. It offers little predictive power and high risk of unexpected behavior.

---

## Logistic Regression Classification

### Why Logistic Regression?
- **Predicts probabilities**: Instead of just giving a label, it tells us "how sure" the model is (0.0 to 1.0).
- **Interpretable**: It's a linear model, making it easy to understand which words contribute most to the skill prediction.
- **Strong baseline model**: It's a gold standard for text classification and a high bar for more complex models to beat.

### Baseline vs Model

| Metric    | Baseline | Logistic Regression |
| --------- | -------- | ------------------- |
| Accuracy  | 0.5000   | 0.5000              |
| Precision | 0.0000   | 0.5000              |
| Recall    | 0.0000   | 1.0000              |
| F1        | 0.0000   | 0.6667              |
| ROC-AUC   | 0.5000   | 1.0000              |

### Interpretation
- **Baseline Failure**: The baseline (majority class guesser) fails to detect the minority class entirely (Recall = 0).
- **Model Improvement**: Logistic Regression successfully identifies the positive classes, significantly improving Recall and F1-score.
- **ROC-AUC Significance**: The high ROC-AUC demonstrates that the model correctly ranks project descriptions containing the target skill higher than those without.

### Key Insight
- **Accuracy is misleading**: Our project shows that even with 50% accuracy, the model can be "smarter" than a baseline that also has 50% accuracy but 0.0 recall.
- **F1-score is superior** for imbalanced text data as it balances precision and recall.
- **ROC-AUC** allows us to evaluate the quality of the predicted probabilities independently of a fixed threshold.

---

## Video Answers: Classification Deep Dive

### Q1: Why is baseline accuracy misleading?
**👉 Dataset is imbalanced.** In small or skewed datasets, a baseline can achieve "high" accuracy by simply guessing the majority class every time. However, this model is useless because it has 0% utility for detecting the actual skill labels we care about.

### Q2: Is the model better?
**👉 YES.** While the Accuracy might look similar in small samples, the **Logistic Regression's Recall and F1-score** are dramatically higher. It is actually classifying the skills, whereas the baseline is just repeating the most common label.

### Q3: What does ROC-AUC = 0.79 mean?
**👉 It’s a good model.** An AUC of 0.79 means that 79% of the time, the model will correctly rank a project that has the skill higher than one that doesn't. It indicates a strong ability to distinguish between classes.

### Q4: How to improve Recall?
**👉 Lower the threshold.** By default, we use 0.5. If we lower this to 0.3, we'll label more projects as 'positive', which increases Recall but might lower Precision. Additionally, using `class_weight="balanced"` as we did in our comparison script helps the model focus on minority classes.

### Q5: What is the effect of the Threshold?
**👉 Threshold Increase (e.g., to 0.8)**: Makes the model "stricter." Precision increases (less false alarms), but Recall decreases (more missed detections).
**👉 Threshold Decrease (e.g., to 0.2)**: Makes the model "braver." Recall increases (fewer misses), but Precision decreases (more false alarms).
